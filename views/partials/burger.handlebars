<div class="burger d-flex w-100 justify-content-between">
  <p>
    {{burger_name}}
  </p>
  {{#unless devoured}}
  <button class="btn btn-dark devour" data-id="{{id}}">Devour</button>
  {{/unless}}
</div>